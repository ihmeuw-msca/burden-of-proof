#' Define global constants to be used for the low hemoglobin BoP pipeline.

effect_measure <- c("Relative risk (RR)","Odds ratio (OR)")
pipeline_type <- "continuous"
rei_id <- 376
risk_dir <- "risk_hemoglobin"
